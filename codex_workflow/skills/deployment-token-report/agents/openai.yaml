interface:
  display_name: "Deployment Token Report"
  short_description: "On-demand local usage accounting"
  default_prompt: "Use $deployment-token-report for the explicitly requested task/window. Preserve accounting warnings."
policy:
  allow_implicit_invocation: false
